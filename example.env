# Copy to ~/.cml.env and fill in. chmod 600 it. Never commit the real one.
CML_HOST=192.168.2.41
CML_USER=admin
CML_PASS=change-me

# Optional: skip TLS verification for CML's self-signed cert (default: true)
CML_VERIFY_SSL=false
